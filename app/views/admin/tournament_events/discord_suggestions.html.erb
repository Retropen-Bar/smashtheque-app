<p>
  Ici le serveur Discord indiqué sur le tournoi est le même que celui de la série.
</p>

<%= render  'suggestions',
            display_tournament_event: ->(tournament_event) {
              smashgg_event = tournament_event.bracket.admin_decorate
              discord_guild = @discord_guilds[tournament_event['suggested_recurring_tournament_discord_guild_id']].admin_decorate

              <<-HTML
              <div class="linked-to">
                #{smashgg_event.tournament_smashgg_link}
                <div class="linked-to">
                  #{discord_guild.admin_link(size: 16)}
                </div>
              </div>
              HTML
            },
            display_suggested_recurring_tournament: ->(tournament_event) {
              discord_guild = @discord_guilds[tournament_event['suggested_recurring_tournament_discord_guild_id']].admin_decorate

              <<-HTML
              <div class="linked-to">
                #{discord_guild.admin_link(size: 16)}
              </div>
              HTML
            } %>
