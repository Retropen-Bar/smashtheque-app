<div class="card community shadow-sm">
  <div class="card-body">
    <div class="float-right">
      <i class="fas fa-users"></i>
      <%= community.players.count %>
    </div>
    <%= link_to community.name, community %>
  </div>
  <%= admin_edit_link(community) %>
</div>
